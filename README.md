# Python Weather App

A simple command-line application to fetch and display the current weather using Python. This project uses the API from [WeatherAPI.com](https://www.weatherapi.com/).

## Setup

1.  Clone the repository.
2.  Install dependencies:
    ```bash
    pip install -r requirements.txt
    ```

## Usage

Run the script from your terminal:

```bash
python main.py
